[toolchain]
# Pinned MSRV (PLAN D1). Every crate also declares `rust-version = "1.83"`.
channel = "1.83.0"
components = ["rustfmt", "clippy"]
targets = ["x86_64-unknown-linux-gnu"]
profile = "minimal"
